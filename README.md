# ArXiv Assistant 📚

Веб-приложение для поиска научных статей в базе данных arXiv и получения информации о них при помощи RAG.

### Локальный запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/PETEPEtrek/ArxivAssistant.git
   cd ArxivAssistant
   ```
2. **Активируйте окружение conda (хотя бы python=3.9)**
   ```bash
   conda create -n arxiv_assistant python=3.9
   conda activate arxiv_assistant
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Убедитесь что Ollama запущен локально (либо используйти openai модель в селекторе, нужно будет добавить ключ для авторизации):**
   ```bash
   ollama serve
   ```

4. **Запустите приложение:**
   ```bash
   streamlit run main.py
   ```

### Запуск через Docker (не до конца доделал, могут быть проблемы с мощностями для модели, лучше через conda)

1. **Запустите все сервисы:**
   ```bash
   docker-compose up -d
   ```

2. **Откройте браузер** и перейдите по адресу `http://localhost:8501`

## Использование

<img width="1280" height="725" alt="image" src="https://github.com/user-attachments/assets/8885df29-a4e0-4ccf-8ed0-39f56d3691c2" />
**На главной странице можно:**
1. Скачать собственный pdf файл со статьей и работать непосредственно с ней
2. Найти статью при помощи arxiv api (поиск можно делать по названию работы, по именам авторов статьи)<img width="1280" height="727" alt="image" src="https://github.com/user-attachments/assets/b21f25d6-9e0b-4dba-b888-8c6d96bf5c80" /> <img width="1280" height="691" alt="image" src="https://github.com/user-attachments/assets/dc577f64-a262-4699-b174-ad3e7360a650" />
При попадании на страницу статьи, начинается ее RAG обработка (либо со статьей уже можно работать, если мы ранее ее обрабатывали и она есть в кэше). Вообще моя идея состояла в том, что, так как мы хотим, чтобы каждый ответ модели сопровождался соответствующим референсом к статье, то лучше всего будет показывать непосредственно секцию, откуда мы взяли информацию. Но как нам достать секцию?
Моя идея состоит в том, чтобы сначала попытаться распарсить latex файл статьи (если он есть, его не всегда загружают) - так это по сути специальный код, то с ним работать проще (можно сразу искать \section, \subsection итд), либо если этого файла нет, работать с pdf - сначала спарсить ее в текст, потом найти стандартные секции (Abstract, Introdauction, etc.) + те, которые выделены как отдельное предложение знаками переноса, имеют в начале цифры или буквы (это уже для пунктов из Appendix), но это по идее работает не так точно. После этого, при помощи recursive text splitterа из langchain делим секции на чанки и добавляем в FAISS, использую FlatIndex так как данных немного, у каждого чанка есть метаинфа, в том числе к какой секции принадлежит. Для поиска использовал bm25 (поэтому стоит задавать вопросы на английском) + плотный поиск по эмбеддингам. 
**Выбрав статью, можно:**
1. Выбрать модель (сделал поддержку openai (можно либо вставить ключ в .env файл, либо непосредственно в ui), либо же можно через ollama (если модель уже скачана вами))<img width="1280" height="327" alt="image" src="https://github.com/user-attachments/assets/f80a873d-5d59-4d2c-9f5f-5ae98bc3f2a4" />
2. После того, как rag индекс пополнен, можно по кнопке попросить суммаризацию статьи, таким образом каждая распаршенная секция статьи будет обработана суммаризатором, и на выходе будет краткое изложение + можно посмотреть ориг текст (берем все чанки секции, объединяем и подаем в llm) <img width="1280" height="416" alt="image" src="https://github.com/user-attachments/assets/37ce2915-016d-4adb-8381-ac4494a2ed94" /> <img width="1280" height="706" alt="image" src="https://github.com/user-attachments/assets/e75d9fce-412f-42fd-9f20-a8f3340a5b06" />

3. Можно общаться с llm по статье, каждый запрос подается в векторную бд, находится самый релевантный чанк и на основе секции, к которой принадлежит чанк (а также метаинфы по типу названия работы, авторов, etc.) модель выдает ответ с референсом к нужной секции. Также в промпт подается история диалога между юзером и ассистентом, при overflow ранняя часть диалога суммаризуется <img width="1280" height="532" alt="image" src="https://github.com/user-attachments/assets/341777ee-fbcd-49bf-9be0-e450d880c069" />

# Что стоит доделать:
1. Запуск через docker контейнер, надо разобраться с использованием памяти для llm
2. Поправить парсинг - он не идеальный и порой либо не находит секции, либо находит плохо (может доставать что-то из обычного текста)
3. Асинхронная обработка статьи иногда сбоит, особенно при latex обработке
